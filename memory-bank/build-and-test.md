# Сборка и тесты

Базовые команды из каталога workspace:

```text
dotnet restore workspace.slnx
dotnet build workspace.slnx
dotnet test workspace.slnx
```

Тестовые проекты пока не добавлены, поэтому `dotnet test` не выполняет тестовых
сборок.
